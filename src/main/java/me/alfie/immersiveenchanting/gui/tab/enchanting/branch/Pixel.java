package me.alfie.immersiveenchanting.gui.tab.enchanting.branch;

/**
 * A single pixel in a baked {@link BranchTexture} connector line, in canvas coordinates.
 */
public record Pixel(int x, int y, int color) {
}
